apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-eks-jenkins 
  labels:
    appName: testwebapp
spec:
  selector:
    matchLabels:
        appName: testwebapp
  replicas: 2
  template:
    metadata:
      labels:
        appName: testwebapp
    spec:
      containers:
        - name: testwebapp
          image: index.docker.io/gvkr1409/testwebapp:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          imagePullPolicy: Always
      imagePullSecrets:
        - name: docker-creds
---
apiVersion: v1
kind: Service
metadata:
    name: my-svc-test
spec:
    selector:
        appName: testwebapp
    type: NodePort
    ports:
    - port: 8089
      targetPort: 8080
