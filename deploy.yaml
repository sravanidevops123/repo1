---
- hosts: localhost
  tasks:
    - name: Building Docker image
      shell: docker build -t testwebapp:v1 .
    - name: Deploy the container
      shell: docker run -itd -p 8080:8080 testwebapp:v1
